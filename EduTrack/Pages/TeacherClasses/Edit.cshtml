@page "{id:int}"
@model EduTrack.Pages.TeacherClasses.EditModel

@{
    ViewData["Title"] = "Edit Teacher Class";
}

<div class="container-fluid mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-gradient bg-success text-white">
                    <h5 class="mb-0"><i class="bi bi-pencil-square me-2"></i>Edit Teacher Class Assignment</h5>
                </div>

                <div class="card-body p-4">
                    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                    {
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            @Model.ErrorMessage
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <form method="post" class="needs-validation">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger d-none" id="validationAlert"></div>
                        <input type="hidden" name="TeacherClassViewModel.Teacher_Class_Id" value="@Model.TeacherClassViewModel.Teacher_Class_Id">

                        <div class="mb-3">
                            <label for="Teacher_Id" class="form-label fw-bold">Teacher <span class="text-danger">*</span></label>
                            <select class="form-select" id="Teacher_Id" name="TeacherClassViewModel.Teacher_Id" required>
                                <option value="">-- Select Teacher --</option>
                                @foreach (var teacher in Model.Teachers)
                                {
                                    <option value="@teacher.Teacher_Id" selected="@(teacher.Teacher_Id == Model.TeacherClassViewModel.Teacher_Id)">@teacher.FullName</option>
                                }
                            </select>
                            <span class="text-danger small">@Html.ValidationMessageFor(m => m.TeacherClassViewModel.Teacher_Id)</span>
                        </div>

                        <div class="mb-3">
                            <label for="Class_Id" class="form-label fw-bold">Class <span class="text-danger">*</span></label>
                            <select class="form-select" id="Class_Id" name="TeacherClassViewModel.Class_Id" required>
                                <option value="">-- Select Class --</option>
                                @foreach (var cls in Model.Classes)
                                {
                                    <option value="@cls.Class_Id" selected="@(cls.Class_Id == Model.TeacherClassViewModel.Class_Id)">@cls.ClassName - @cls.Section</option>
                                }
                            </select>
                            <span class="text-danger small">@Html.ValidationMessageFor(m => m.TeacherClassViewModel.Class_Id)</span>
                        </div>

                        <div class="mb-3">
                            <label for="Subject_Id" class="form-label fw-bold">Subject <span class="text-danger">*</span></label>
                            <select class="form-select" id="Subject_Id" name="TeacherClassViewModel.Subject_Id" required>
                                <option value="">-- Select Subject --</option>
                                @foreach (var subject in Model.Subjects)
                                {
                                    <option value="@subject.Subject_Id" selected="@(subject.Subject_Id == Model.TeacherClassViewModel.Subject_Id)">@subject.Subject_Name (@subject.Subject_Code)</option>
                                }
                            </select>
                            <span class="text-danger small">@Html.ValidationMessageFor(m => m.TeacherClassViewModel.Subject_Id)</span>
                        </div>

                        <div class="mb-3">
                            <div class="form-check form-switch">
                                <input class="form-check-input" type="checkbox" id="IsActive" name="TeacherClassViewModel.IsActive" value="true" checked="@Model.TeacherClassViewModel.IsActive">
                                <label class="form-check-label" for="IsActive"><strong>Active</strong></label>
                            </div>
                        </div>

                        <div class="mb-3 text-muted small">
                            <div><strong>Created:</strong> @Model.TeacherClassViewModel.Created_By - @Model.TeacherClassViewModel.Created_Date.ToString("dd/MM/yyyy HH:mm")</div>
                            <div><strong>Modified:</strong> @Model.TeacherClassViewModel.Modified_By - @Model.TeacherClassViewModel.Modified_Date.ToString("dd/MM/yyyy HH:mm")</div>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-success btn-lg"><i class="bi bi-check-circle me-2"></i>Update Assignment</button>
                            <a asp-page="Index" class="btn btn-outline-secondary"> <i class="bi bi-arrow-left me-2"></i>Back to List</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        if (document.querySelectorAll('.text-danger').length > 0) { var el = document.getElementById('validationAlert'); if (el) el.classList.remove('d-none'); }
    </script>
}
